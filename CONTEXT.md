# Matrox Frame Grabber

Matrox Rapixo CXP(CoaXPress) 프레임그래버 보드 한 장으로 차량용 **AVN** 화면을 고속 카메라로
감시해, 화면 이상을 검지하고 그 **전후 구간을 파일로 남기는** 데스크톱 프로그램. 라이브 뷰와
수동 녹화는 그 위에 함께 있다. 이 글로서리는 코드·UI·문서에서 쓰는 말을 하나로 고정해, 같은
것이 두 이름으로 불리지 않게 한다.

용어 이름은 코드 식별자와 대응시키기 위해 영문으로 둔다.

## 용어

### 감시 대상

**AVN screen**
이 앱이 감시하는 대상. 차량용 AVN(오디오·비디오·내비게이션) 시스템이 출력하는 화면이며,
카메라가 보는 것이 이것이다. "디스플레이"는 **우리 앱의 표시 경로**를 뜻하므로 감시 대상에는
쓰지 않는다.
_피할 말_: 디스플레이, 모니터, DUT, 화면(단독으로 — 우리 화면과 구분되지 않는다)

**Operator**
장비를 현장에서 조작하는 사람. 이상 판정의 임계값을 맞추고 분석 ROI를 지정하는 주체다.
_피할 말_: 사용자, 관리자, 작업자

### 취득

**Channel**
보드의 취득 슬롯 하나. 카메라가 붙어 있든 아니든 **항상 네 개**다.
_피할 말_: digitizer, port, slot, 슬롯을 가리키는 "카메라"

**Camera**
채널에 연결된 실제 CoaXPress 장치. 채널에 카메라가 없을 수 있으며, 빈 채널은 오류가 아니라
정상 상태다.
_피할 말_: device, source

**Port tag**
채널을 현장의 케이블과 맞춰 보기 위한 고정 표기(CAM0~CAM3). 채널의 이름표일 뿐 별개 개념이
아니다 — 세거나 지칭할 때는 Channel을 쓴다.
_피할 말_: 포트를 채널 대신 쓰는 것, 포트 번호

**DCF**
특정 카메라의 신호를 보드가 어떻게 해석할지 지정하는 카메라 설정 파일. 채널의 DCF가 그 채널의
프레임이 컬러인지 여부 등을 결정한다.
_피할 말_: 설정 파일, 카메라 프로파일

**Grab**
카메라에서 프레임을 연속으로 취득하는 상태. 채널은 grab 중이거나 정지 상태이며, 녹화는 grab
중일 때만 가능하다.
_피할 말_: 캡처, 스트리밍, 라이브 뷰

**Camera trigger**
카메라가 프레임 취득을 시작하는 계기(트리거 모드·소스·소프트 트리거). **이상 검지 쪽의
계기와는 다른 개념이다** — 그쪽은 *Detection*이라 부르고, "트리거"라는 말은 카메라에 남겨 둔다.
_피할 말_: 그냥 "트리거"로 두 개를 섞어 쓰는 것

**Exposure time**
카메라가 한 프레임을 위해 빛을 모으는 시간(`ExposureTime`, µs). 물리적으로는 그 구간의 **적분**이므로,
노출보다 짧은 사건은 사라지지 않고 비례해서 옅어진다 — Dropout의 depth와 PWM 소거가 모두 이 성질
하나에서 나온다. 그래서 이것은 밝기 노브가 아니라 **검출기 파라미터**이며, 채널의 최대 fps도
여기서 정해진다.
_피할 말_: 셔터 스피드, 셔터 시간, 익스포저. 물리를 말할 때는 "적분 시간"이 더 정확하다

**Bayer mosaic**
컬러 센서가 실제로 만들어내는 단일 밴드 이미지. 픽셀마다 색 하나씩만 담고 있다. 이것을 3밴드
컬러로 바꾸는 것이 *Bayer conversion*이다.
_피할 말_: raw 이미지(RAW recording과 혼동), 흑백, 모노

**Bayer conversion**
프레임이 호스트에 도달하기 전에 보드가 하드웨어로 Bayer mosaic를 컬러로 바꾸는 단계.
세션이 아니라 **보드에 남는 영속 설정**이다.
_피할 말_: 디베이어, 디모자이크, 컬러 변환

**Acquisition ROI**
센서가 실제로 읽어내는 화소 범위(`OffsetX` / `OffsetY` / `Width` / `Height`). 페이로드가 이것에
비례해 줄어드는 이유는 받은 이미지를 잘라내기 때문이 아니라 **애초에 그만큼만 읽기** 때문이다
(벤더 문서의 windowing · partial readout이 같은 것을 가리킨다). **이 카메라는 이 쓰기를 받아들이고
무시한다.**

Analysis ROI와 이름이 겹치므로 **어느 쪽인지 반드시 붙여서 말한다** — 둘은 층도 효과도 다르다.
취득 ROI는 대역폭을 줄이고, 분석 ROI는 판정 품질을 올린다.
_피할 말_: 크롭(받은 이미지를 잘라내는 편집 동작이라 취득 단계를 잘못 설명한다), ROI(단독)

**Decimation**
카메라가 화소를 건너뛰어 프레임 자체를 작게 만드는 것(1·2·4). 이 카메라에서 **페이로드를 줄이는
유일한 수단**이다 — Acquisition ROI 쓰기는 받아들여지고 무시된다. 시야는 그대로이고 해상도만 낮아진다.
**카메라에 영속되는 설정**이다.
_피할 말_: 크롭, ROI(단독), 축소, 스케일, 리사이즈

**Payload**
프레임 한 장이 버스를 건너는 바이트 수. decimation과 밴드 수(컬러 3, Bayer mosaic 1)로 정해진다.
대역폭 논의의 단위는 해상도가 아니라 이것이다.
_피할 말_: 프레임 크기, 이미지 크기

**Host DMA ceiling**
채널 전부가 **함께** 쓰는 취득 대역폭 상한. 채널 수와 무관하게 합계가 여기서 고정되고, 초과분은
missed frame이 된다. 카메라 한 대당 별도로 걸리는 CXP 링크 상한과는 다른 층이다.
_피할 말_: 링크 대역폭, 대역폭(어느 층인지 밝히지 않고)

### 표시

**Pane**
채널 하나를 그 채널 전용 조작부와 함께 보여주는 화면 타일. 채널마다 하나씩 항상 네 개다.
_피할 말_: 타일(Tile이 다른 뜻으로 쓰인다), 셀, 뷰, 창

**취득 fps / 표시 fps**
취득 fps는 채널이 카메라에서 프레임을 받는 속도, 표시 fps는 그 프레임이 화면에 다시 그려지는
속도. **두 값은 독립이다** — 표시를 낮추는 것은 CPU를 아끼는 일이고 취득 fps를 늘려 주지 않는다.
어느 쪽인지 밝히지 않은 "fps"는 이 프로젝트에서 항상 오해를 만든다.
_피할 말_: fps(단독), 프레임레이트(단독)

**Snapshot**
녹화와 무관하게 채널의 현재 화면에서 정지 이미지 한 장을 저장한 것.
_피할 말_: 스크린샷, 스틸, 캡처

### 분석

**Analysis ROI**
지표를 계산할 화소 범위. 취득된(이미 decimated) 프레임 좌표로 지정하며, 기본값은 전체 프레임이다.
**카메라 설정이 아니라 소프트웨어 값**이라는 점이 이 용어의 존재 이유다 — 하드웨어가 거부할 수
없고 재할당도 필요 없으며, 잘못 잡아도 되돌리는 비용이 0이다. 대역폭은 이것으로 줄지 않는다
(그건 decimation의 몫이다). 판정 품질 — 배경을 지표에서 빼는 일 — 이 이것의 몫이다.
_피할 말_: 크롭, 카메라 ROI, 취득 ROI, 관심영역

**Tile**
분석 ROI를 격자로 나눈 한 칸. 지표는 타일 단위로 축약된다. 프레임 전역 값 하나로는 부분 백화가
정상 범위에 묻히기 때문에 존재한다.
_피할 말_: 블록, 셀, 격자, pane

**Brightness reading**
한 시점의 밝기 측정 결과 하나(luma + clipped ratio + black ratio). 그래프의 점 하나가 이것이다.
_피할 말_: 샘플(추출하는 **행위**와 구분되지 않는다), 프레임 밝기

**Luma**
Rec.601 가중으로 계산한 밝기(0~255). Bayer mosaic의 단순 평균은 CFA의 1R:2G:1B 비율에 따라
0.25R + 0.50G + 0.25B가 되는데, 이것이 601에 충분히 가까워서 RAW recording으로 밴드가 1개로
바뀌어도 그래프가 이어진다.
_피할 말_: 휘도, 그레이값, 밝기(밝기는 상위 개념이다)

**Clipped ratio / Black ratio**
값이 255인 화소의 비율 / 0인 화소의 비율. 평균 luma가 미동하지 않는 고장(절반이 날아가고 절반이
어두워지는 경우)을 잡기 위한 보조 지표다.
_피할 말_: 포화율·흑화율을 서로 바꿔 쓰기, 클리핑(단독)

**Backlight PWM**
AVN 패널의 LED 백라이트를 빠르게 켜고 끄며 그 **듀티비로 밝기를 정하는 방식**(보통 200 Hz~2 kHz).
화면 내용은 그대로이고 조명만 점멸하므로 측정된 luma에 **가짜 변동**을 만든다. **이 프로젝트에서
"PWM"은 항상 이것을 가리킨다** — 패널의 화면 주사(60 Hz)나 실내 조명의 전원 리플과는 층이 다르다.
패널을 어둡게 할수록 듀티가 짧아져 변동이 커지고, 노출을 그 주기의 정수배로 맞추면 적분이
정확히 소거한다.
_피할 말_: 깜박임(Flicker·Dropout과 섞인다), 패널 주사, 리프레시, 플리커

**Mains ripple**
실내 조명이 전원 주파수에 따라 밝기를 떠는 것. **국내 60 Hz 계통에서는 120 Hz**다. 패널 자체가
아니라 **반사광**으로 측정에 들어오므로, 노출 정합에 의존하기보다 차광·조명 조절로 없애는 것이
정석이다 — 백라이트 PWM과 둘 다 노출로 소거할 수는 없다(둘을 동시에 죽이는 노출은 25 ms뿐이고
그러면 40 fps가 된다).
_피할 말_: PWM(백라이트와 층이 다르다), 플리커, 조명 노이즈

### 이상

**Anomaly**
AVN screen이 정상 동작에서 벗어난 것. **지속형**(Washout, Blackout, Flip)과 **사건형**(Dropout)이
있고 확정 방식이 서로 다르다 — 지속형은 히스테리시스로 콘텐츠 변화와 가르고, 사건형은 프레임
하나로도 확정되어야 한다. 그래서 둘을 한 상태머신으로 묶을 수 없다.
_피할 말_: 에러, 결함(제품 결함과 혼동), 알람

**Washout**
밝은데 **구조가 없는** 이상. 판정의 핵심은 밝기가 아니라 구조의 붕괴다 — 정상적으로 밝은 화면
(흰 배경 UI, 카메라 앞의 흰 물체)과 평균 밝기로는 구분되지 않는다.
_피할 말_: 화이트아웃, 포화, 과노출, 백화(한글 단독 — "백화현상"은 설명에서만)

**Flicker**
프레임별 밝기의 시간축 변동. 백라이트 PWM이 만드는 **가짜 깜박임**과 구분해야 하는 것이 이
용어의 어려움 전부다.
_피할 말_: 깜박임(현장의 "깜박임"은 대개 Dropout이다), 떨림, 지터, 노이즈, 프레임 드랍

**Blackout**
화면이 꺼진 채 **지속되는** 이상. 평균 luma와 black ratio로 잡힌다. 같은 현상이 순간적으로만
나타나면 그것은 Dropout이며, 둘을 가르는 것은 밝기가 아니라 **지속 시간**이다.
_피할 말_: 블랙 프레임, 무신호, 신호 없음, dropout

**Dropout**
화면이 **순간적으로** 꺼졌다 곧 돌아오는 이상. 현장에서 가장 빈번하게 신고되는 이상이며,
현장에서 "깜박인다"고 말하는 것은 대개 Flicker가 아니라 이것이다. 상태가 아니라 **사건**이라서
지속형 이상과 같은 히스테리시스로 다룰 수 없고, 보고 단위도 상태가 아니라 발생 시각·지속 시간·
깊이다.
_피할 말_: 깜박임(Flicker와 섞인다), 블랙아웃(지속형과 섞인다), 프레임 드랍(인코딩 측 손실),
freeze(멈춤은 다른 고장)

**Flip**
화면이 상하·좌우로 뒤집힌 이상. **밝기 계열 스칼라 지표로는 원리적으로 검출되지 않는다**(모든
스칼라 통계가 뒤집기에 불변이다). 타일 배치를 비교해야 한다. 색이 뒤바뀌는 고장은 별개다.
_피할 말_: 미러링, 회전, 색 반전, RGB 반전

**Detection**
이상이 **확정되는 사건**. 이상 구간 추출을 시작하는 것이 이것이다. Camera trigger와 다른
개념이며, 혼동을 피하려고 이쪽에는 "트리거"라는 말을 쓰지 않는다.

한국어로는 **검지**로 옮기고 동사는 **검출한다**를 쓴다. 다만 **"검지"만으로 파이프라인을
가리키지 않는다** — 사건은 Detection이고, 그 사건을 만들어 내는 경로는 "검지 경로"라고 붙여서
쓴다(그래프 경로 · 표시 경로와 나란한 이름이다). 한쪽은 한 시점이고 다른 쪽은 사슬이라, 같은
말로 두면 "검지가 독립이다"가 무엇이 독립인지 말해 주지 않는다.
_피할 말_: 트리거, 알람, 검출(명사 용법), 감지(카메라 분리 감지에만 쓴다), 검지(단독으로 경로를
가리키는 용법)

### 프레임과 지표의 손실

셋은 원인도 층도 서로 다르다. **한쪽 말을 다른 쪽에 쓰지 않는다.**

**Missed frame**
보드는 취득했으나 애플리케이션이 받을 준비가 되지 않아 넘겨받지 못한 프레임. **취득 측** 손실.
_피할 말_: dropped frame, 유실 프레임, 누락 프레임

**Dropped frame**
애플리케이션이 받았으나 인코더가 너무 밀려서 **의도적으로 버린** 프레임. **인코딩 측** 손실이며
결함이 아니라 받아들인 트레이드오프다.
_피할 말_: missed frame, 유실 프레임

**Skipped metric**
프레임은 정상으로 처리했으나 분석이 밀려서 버린 **축약 결과**. 프레임이 아니라 지표의 손실이고,
몇 개 빠져도 판정이 무너지지 않기 때문에 허용된다 — 다만 조용히 버리지 않고 센다.
_피할 말_: dropped frame, missed frame, 샘플 유실

### 녹화

**Recording**
채널의 프레임을 파일로 쓰는 일 전반을 가리키는 상위 개념. 한 채널은 동시에 최대 하나의 녹화
모드에만 있을 수 있다.
_피할 말_: 캡처, session("session"은 녹화를 구현하는 객체 이름으로 남겨 둔다)

**Live recording**
라이브 뷰를 우선하는 녹화 모드. 화면에 보이는 그대로의 컬러 이미지를 기록하며, 프리뷰가
끊기느니 프레임을 버린다.
_피할 말_: 컬러 녹화, 일반 녹화, Rec 모드

**RAW recording**
완전성을 우선하는 녹화 모드. 센서의 Bayer mosaic를 원본 해상도 그대로 기록하며, 프레임을
잃느니 화면이 끊기는 쪽을 택한다.
_피할 말_: Bayer 녹화, RAW 모드

**Segment**
RAW recording을 시간으로 잘라낸 한 조각. RAW recording은 연속된 segment의 나열이고, 각
segment는 끝나는 즉시 개별적으로 변환된다.
_피할 말_: 청크, 조각, 클립(Anomaly clip과 혼동)

**Preroll**
Detection **이전** 구간을 남기기 위해 평상시에 계속 보유하는 최근 segment들. 설정값은 초 단위이고
보유 개수는 segment 길이에서 파생된다 — segment 길이를 바꿨을 때 프리롤 시간이 조용히 달라지지
않게 하기 위한 것이다.
_피할 말_: 버퍼, 캐시, 히스토리, 링

**Anomaly clip**
Detection이 확정한 전후 구간을 출력 폴더에 남긴 결과물. 운영자가 시작한 녹화가 아니라 **판정이
만든 산출물**이라는 점이 다른 녹화물과 구분된다.
_피할 말_: 이벤트 영상, 스니펫, 하이라이트, 세그먼트

**Scratch**
변환이 끝나기 전까지 RAW segment를 담아 두는 로컬 저장소. 출력 폴더와 구분되며, 의도적으로
네트워크 위치를 쓰지 않는다.
_피할 말_: 임시 폴더, 스테이징, 캐시

**Output folder**
변환이 끝난 녹화물과 스냅샷 등 최종 산출물을 쓰는 위치. 네트워크 위치일 수 있다.
_피할 말_: 저장 폴더, 대상 폴더

**Resolution preset**
스냅샷과 live recording에 적용되는 **출력** 해상도(원본 / 1080p / 720p). 취득 해상도와 무관하며
RAW recording에는 적용되지 않는다.
_피할 말_: 화질, 해상도(단독 — 취득 해상도와 혼동), 스케일
